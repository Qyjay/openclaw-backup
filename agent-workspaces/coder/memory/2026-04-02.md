# 2026-04-02 Daily Log — BB

## 日迹前端改动

### TASK-F 对话自动转素材（已完成，延续昨天工作）
- 前端 commit `977d60d`，后端 commit `85d117a`，均已 push
- 前后端文档已同步更新

### Mock 数据修复
- `src/services/mock/material.ts`：所有素材 date 改为 `new Date().toISOString().split('T')[0]`（当天动态日期），修复 write 页只显示对话素材的问题
- 首页「那年今日」卡片补上 `@click="goDetail(todayHistory[0].diary.id)"`，之前缺 click 事件

### 广场页三项改动（discover/index.vue）
1. **下拉刷新**：scroll-view 加 `refresher-enabled`，触发时重置 page=1 + 重拉帖子 + 分身状态
2. **工具箱上移**：从信息流底部（帖子加载完后）移至搜索栏下方，进页面即可见
3. **页内搜索**：分身状态条下方新增搜索输入框，300ms 防抖，匹配 content/tags/authorName/location
   - Mock 模式：前端本地过滤
   - 真实模式：`GET /api/plaza/posts?q=keyword` 后端搜索（后端待实现）
   - NavBar 右侧搜索图标已移除（搜索改为页内）

### API 层改动
- `src/services/api/plaza.ts`：`getPlazaPosts()` 新增 `keyword` 可选参数
- `src/services/mock/plaza.ts`：Mock 支持 keyword 本地过滤

### 文档更新
- 前端：`DESIGN.md`（新增 §3.12 广场搜索与刷新）、`architecture.md`
- 后端：`API-DOCS.md`（plaza/posts 新增 `q` 查询参数说明）
- 前端 `README.md` 大更新：核心功能表新增广场/分身/对话转素材，组件 5→6，API 模块 8→10，目录结构同步

### Git 提交
- 前端：`b34a578`（广场改动+文档）+ README 更新 commit，均已 push main
- 后端：`2cf64a7`（API-DOCS q 参数），已 push main

## 项目状态
- **仓库**：`github.com/Qyjay/riji-frontend` / `github.com/Qyjay/riji-backend`
- **本地路径**：`~/projects/riji-frontend` / `~/projects/riji-backend`
- **前端测试**：10132 passed
- **比赛截止**：2026-05-11

## Open TODOs（carry forward）
- [ ] 广场+分身 16 个后端接口未实现（TASK-E）
- [ ] 日记搜索功能待实现
- [ ] 每日自动生成日记定时任务（23:00）
- [ ] 首页日记点击详情不显示（疑似 Mock 日记 id 与 detail 页数据不匹配）
- [ ] v2 话题切换自动标记素材
