# 2026-04-01 — BB 日记

## 日迹 App 后端（riji-backend）

### 广场+分身数据库建设（完成）
- vivid-cloud session（昨天派出）跑了一半就挂了：plaza.py 和 avatar.py 模型文件创建了但没 commit，种子数据没建
- Claude Code API 今天持续不可用（pre-flight check 超时），两次 spawn 都失败
- **自己动手完成了全部工作：**
  - `scripts/seed_plaza_avatar.py` — 展示级种子数据
    - 10 个测试用户（密码统一 test123456）：linxiaohan/zhoucheng/summer_z/wangfuai/liuyang_c/chenmo/yuxin_r/leomao/zhouqian/hanxiao
    - 15 条广场帖子（buddy×5/help×5/share×3/dating×2）
    - 28 条评论（含 5 条分身评论）
    - 39 条点赞
    - 41 条分身记忆（每用户 3-5 条，覆盖 fact/interest/personality/need/habit/relation）
    - 10 条分身状态
    - 20 条分身推荐匹配
    - 10 条分身侧写
  - 修复了一个 Python 作用域 bug：函数内部的 import 导致模块级 import 的名字被当作局部变量
  - commit `4faf671` — feat: 广场+分身数据模型 + 展示级种子数据
  - 已验证 seed 脚本运行全绿

### 文档同步更新（完成）
- README.md — 项目结构补 plaza/avatar，新增测试账号说明，scripts 补 seed_plaza_avatar.py
- CLAUDE.md — 项目结构 + 数据模型补 7 张新表
- TASK-E-PLAZA.md — 顶部新增进度表，验收标准前 3 项打勾
- ONBOARDING.md — 初始化步骤补新 seed 脚本和测试账号
- commit `0a8778b`

## 日迹 App 前端（riji-frontend）

### 分身代发标识（完成）
- mock 数据新增 2 条 `isFromAgent: true` 帖子（p12 陈同学分身、p13 张同学分身）
- 列表页（discover/index.vue）作者名旁加 `🤖 分身代发` 橙色标签
- 详情页（detail.vue）同样加标识
- commit `ab86e89`

### 后端 Base URL 配置（完成）
- config.ts — API_BASE_URL 从常量改为动态变量，支持运行时切换 + 本地存储持久化
- request.ts — 改为读取动态 base URL（通过 getter 函数）
- settings.vue 开发者选项新增：后端地址输入框 + 保存/重置/测试按钮
- 用途：后端同学在 APK 中输入局域网地址（如 http://192.168.x.x:8000/api）直接联调
- commit `d6750a7`

## 配置变更
- 移除了 MiniMax M2.7 fallback：`agents.defaults.model.fallbacks: []`
- 所有对话强制使用 `claude-api/claude-opus-4-6-thinking-high`

## 铁则（新增）
### #4 代码改动必须同步更新对应文档（2026-04-01）
- 前辈明确要求：以后所有改动自动更新 GitHub 上对应的文档
- 每次 commit 代码变更时，检查 README.md / CLAUDE.md / ONBOARDING.md / TASK-*.md 等是否需要同步
- 不要等前辈提醒

## Dev Server
- 前端：http://localhost:5173/（HMR 正常）
- 后端路径：~/projects/riji-backend
